# glpi-voice-bot — dependencias Python
# Instalar con: pip install -r requirements.txt

# LiveKit Agents (núcleo)
# ⚠ Mantener en 0.12.x — la v1.0 tiene una API completamente distinta
livekit-agents==0.12.21

# LiveKit Plugins (explícitamente declarados para evitar problemas de ModuleNotFound)
# ⚠ Cada plugin tiene su propio esquema de versiones (NO coincide con livekit-agents)
livekit-plugins-deepgram==0.7.4
livekit-plugins-cartesia==0.4.12
livekit-plugins-openai==0.12.4
livekit-plugins-silero==0.7.6

# Cliente HTTP asíncrono
httpx>=0.25.0

# Carga de variables de entorno desde .env
python-dotenv>=1.0

# PyAV es necesario para los decodificadores de audio de LiveKit Agents
av>=12.0.0
